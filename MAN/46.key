# $KEY MGMT ###############
LOAD   - move load $KEY
sLOAD  - copy load $KEY 
STACK  - load $KEY && send $KEY tarball
VOLLEY - use current $KEY to send $KEY tarball
GRAB   - transfer $KEY on host
###########################
# wheel group grow users
# wheel adds x grp
# sys_file git confirm change
# more wheel users appended
